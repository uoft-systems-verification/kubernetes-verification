(* autogenerated from kubernetes_model/simpleapiserver *)
Require Export New.code.fmt.
Require Export New.code.k8s_io.api.apps.v1.
Require Export New.code.k8s_io.api.core.v1.
Require Export New.code.k8s_io.apimachinery.pkg.api.errors.
Require Export New.code.k8s_io.apimachinery.pkg.api.meta.
Require Export New.code.k8s_io.apimachinery.pkg.apis.meta.v1.
Require Export New.code.k8s_io.apimachinery.pkg.labels.
Require Export New.code.k8s_io.apimachinery.pkg.runtime.schema.
Require Export New.code.k8s_io.apimachinery.pkg.types.
Require Export New.code.math.rand.
Require Export New.code.strconv.
Require Export New.code.sync.
Require Export New.code.time.

From New.golang Require Import defn.
Definition simpleapiserver : go_string := "kubernetes_model/simpleapiserver".

Module simpleapiserver.

Module KKey. Definition id : go_string := "kubernetes_model/simpleapiserver.KKey"%go. End KKey.
Module IndexFunc. Definition id : go_string := "kubernetes_model/simpleapiserver.IndexFunc"%go. End IndexFunc.
Module Indexers. Definition id : go_string := "kubernetes_model/simpleapiserver.Indexers"%go. End Indexers.
Module State. Definition id : go_string := "kubernetes_model/simpleapiserver.State"%go. End State.

Section code.
Context `{ffi_syntax}.


Definition KKey : go_type := structT [
  "Kind" :: stringT;
  "Name" :: stringT;
  "Namespace" :: stringT
].
#[global] Typeclasses Opaque KKey.
#[global] Opaque KKey.

Definition IndexFunc : go_type := funcT.
#[global] Typeclasses Opaque IndexFunc.
#[global] Opaque IndexFunc.

Definition Indexers : go_type := mapT stringT IndexFunc.
#[global] Typeclasses Opaque Indexers.
#[global] Opaque Indexers.

Definition NamespaceIndex : go_string := "namespace"%go.

Axiom ControllerUIDIndex : go_string.

Definition PodControllerUIDIndex : go_string := "podControllerUID"%go.

Definition OrphanPodIndexKey : go_string := "_ORPHAN_POD"%go.

Definition State : go_type := structT [
  "m" :: mapT KKey interfaceT;
  "uidCounter" :: int64T;
  "resourceVersionCounter" :: int64T;
  "indexer" :: Indexers
].
#[global] Typeclasses Opaque State.
#[global] Opaque State.

Definition stateMu : go_string := "kubernetes_model/simpleapiserver.stateMu"%go.

Definition state : go_string := "kubernetes_model/simpleapiserver.state"%go.

Definition nameRand : go_string := "kubernetes_model/simpleapiserver.nameRand"%go.

Axiom nameRand'init : val.

Definition init : go_string := "kubernetes_model/simpleapiserver.init"%go.

Definition namespaceIndex : go_string := "kubernetes_model/simpleapiserver.namespaceIndex"%go.

(* go: api_server.go:62:6 *)
Definition namespaceIndexⁱᵐᵖˡ : val :=
  λ: "obj",
    exception_do (let: "obj" := (mem.alloc "obj") in
    let: "err" := (mem.alloc (type.zero_val #error)) in
    let: "metaObj" := (mem.alloc (type.zero_val #v1.Object)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#interfaceT] "obj") in
    (func_call #meta.Accessor) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("metaObj" <-[#v1.Object] "$r0");;;
    do:  ("err" <-[#error] "$r1");;;
    (if: (~ (interface.eq (![#error] "err") #interface.nil))
    then
      return: ((let: "$sl0" := #""%go in
       slice.literal #stringT ["$sl0"]), let: "$a0" := #"object has no meta: %v"%go in
       let: "$a1" := ((let: "$sl0" := (![#error] "err") in
       slice.literal #interfaceT ["$sl0"])) in
       (func_call #fmt.Errorf) "$a0" "$a1")
    else do:  #());;;
    return: ((let: "$sl0" := ((interface.get #"GetNamespace"%go (![#v1.Object] "metaObj")) #()) in
     slice.literal #stringT ["$sl0"]), #interface.nil)).

Definition podControllerUIDIndex : go_string := "kubernetes_model/simpleapiserver.podControllerUIDIndex"%go.

Definition OrphanPodIndexKeyForNamespace : go_string := "kubernetes_model/simpleapiserver.OrphanPodIndexKeyForNamespace"%go.

(* go: api_server.go:70:6 *)
Definition podControllerUIDIndexⁱᵐᵖˡ : val :=
  λ: "obj",
    exception_do (let: "obj" := (mem.alloc "obj") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "pod" := (mem.alloc (type.zero_val #ptrT)) in
    let: ("$ret0", "$ret1") := (interface.checked_type_assert #ptrT (![#interfaceT] "obj") #(ptrT.id v1.Pod.id)) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("pod" <-[#ptrT] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then return: (#slice.nil, #interface.nil)
    else do:  #());;;
    (let: "ref" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (interface.make #(ptrT.id v1.Pod.id) (![#ptrT] "pod")) in
    (func_call #v1.GetControllerOf) "$a0") in
    do:  ("ref" <-[#ptrT] "$r0");;;
    (if: (![#ptrT] "ref") ≠ #null
    then
      return: ((let: "$sl0" := (![#types.UID] (struct.field_ref #v1.OwnerReference #"UID"%go (![#ptrT] "ref"))) in
       slice.literal #stringT ["$sl0"]), #interface.nil)
    else do:  #()));;;
    return: ((let: "$sl0" := (let: "$a0" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"Namespace"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "pod")))) in
     (func_call #OrphanPodIndexKeyForNamespace) "$a0") in
     slice.literal #stringT ["$sl0"]), #interface.nil)).

Definition controllerUIDIndex : go_string := "kubernetes_model/simpleapiserver.controllerUIDIndex"%go.

Definition Init : go_string := "kubernetes_model/simpleapiserver.Init"%go.

(* go: api_server.go:93:6 *)
Definition Initⁱᵐᵖˡ : val :=
  λ: <>,
    with_defer: (do:  ((method_call #(ptrT.id sync.Mutex.id) #"Lock"%go (globals.get #stateMu)) #());;;
    do:  (let: "$f" := (method_call #(ptrT.id sync.Mutex.id) #"Unlock"%go (globals.get #stateMu)) in
    "$defer" <-[#funcT] (let: "$oldf" := (![#funcT] "$defer") in
    (λ: <>,
      "$f" #();;
      "$oldf" #()
      )));;;
    let: "$r0" := (map.make #KKey #interfaceT) in
    do:  ((struct.field_ref #State #"m"%go (globals.get #state)) <-[type.mapT #KKey #interfaceT] "$r0");;;
    let: "$r0" := #(W64 0) in
    do:  ((struct.field_ref #State #"uidCounter"%go (globals.get #state)) <-[#int64T] "$r0");;;
    let: "$r0" := #(W64 0) in
    do:  ((struct.field_ref #State #"resourceVersionCounter"%go (globals.get #state)) <-[#int64T] "$r0");;;
    let: "$r0" := ((let: "$v0" := (func_call #namespaceIndex) in
    let: "$k0" := #NamespaceIndex in
    let: "$v1" := (func_call #podControllerUIDIndex) in
    let: "$k1" := #PodControllerUIDIndex in
    let: "$v2" := (func_call #controllerUIDIndex) in
    let: "$k2" := #ControllerUIDIndex in
    map.literal #stringT #IndexFunc [map.kv_entry "$k0" "$v0"; map.kv_entry "$k1" "$v1"; map.kv_entry "$k2" "$v2"])) in
    do:  ((struct.field_ref #State #"indexer"%go (globals.get #state)) <-[#Indexers] "$r0");;;
    return: #()).

(* go: api_server.go:108:6 *)
Definition OrphanPodIndexKeyForNamespaceⁱᵐᵖˡ : val :=
  λ: "namespace",
    exception_do (let: "namespace" := (mem.alloc "namespace") in
    return: (#(OrphanPodIndexKey ++ "/"%go) + (![#stringT] "namespace"))).

Definition objGet : go_string := "kubernetes_model/simpleapiserver.objGet"%go.

Definition objList : go_string := "kubernetes_model/simpleapiserver.objList"%go.

Definition filterByLabelSelector : go_string := "kubernetes_model/simpleapiserver.filterByLabelSelector"%go.

(* go: api_server.go:136:6 *)
Definition filterByLabelSelectorⁱᵐᵖˡ : val :=
  λ: "items" "selector",
    exception_do (let: "selector" := (mem.alloc "selector") in
    let: "items" := (mem.alloc "items") in
    let: "filtered_items" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$range" := (![#sliceT] "items") in
    (let: "val" := (mem.alloc (type.zero_val #interfaceT)) in
    slice.for_range #interfaceT "$range" (λ: "$key" "$value",
      do:  ("val" <-[#interfaceT] "$value");;;
      do:  "$key";;;
      let: "err" := (mem.alloc (type.zero_val #error)) in
      let: "metadata" := (mem.alloc (type.zero_val #v1.Object)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#interfaceT] "val") in
      (func_call #meta.Accessor) "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("metadata" <-[#v1.Object] "$r0");;;
      do:  ("err" <-[#error] "$r1");;;
      (if: (~ (interface.eq (![#error] "err") #interface.nil))
      then return: (![#sliceT] "filtered_items", ![#error] "err")
      else do:  #());;;
      (if: let: "$a0" := (interface.make #labels.Set'.id ((interface.get #"GetLabels"%go (![#v1.Object] "metadata")) #())) in
      (interface.get #"Matches"%go (![#labels.Selector] "selector")) "$a0"
      then
        let: "$r0" := (let: "$a0" := (![#sliceT] "filtered_items") in
        let: "$a1" := ((let: "$sl0" := (![#interfaceT] "val") in
        slice.literal #interfaceT ["$sl0"])) in
        (slice.append #interfaceT) "$a0" "$a1") in
        do:  ("filtered_items" <-[#sliceT] "$r0")
      else do:  #())));;;
    return: (![#sliceT] "filtered_items", #interface.nil)).

Definition objListBySelector : go_string := "kubernetes_model/simpleapiserver.objListBySelector"%go.

Definition randomSuffix : go_string := "kubernetes_model/simpleapiserver.randomSuffix"%go.

Definition deepCopy : go_string := "kubernetes_model/simpleapiserver.deepCopy"%go.

Definition objCreate : go_string := "kubernetes_model/simpleapiserver.objCreate"%go.

Definition objUpdate : go_string := "kubernetes_model/simpleapiserver.objUpdate"%go.

Definition objDelete : go_string := "kubernetes_model/simpleapiserver.objDelete"%go.

Definition Index : go_string := "kubernetes_model/simpleapiserver.Index"%go.

(* Objects returned here must be treated as read-only.

   go: api_server.go:305:6 *)
Definition Indexⁱᵐᵖˡ : val :=
  λ: "kind" "indexName" "obj",
    exception_do (let: "obj" := (mem.alloc "obj") in
    let: "indexName" := (mem.alloc "indexName") in
    let: "kind" := (mem.alloc "kind") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "indexFunc" := (mem.alloc (type.zero_val #IndexFunc)) in
    let: ("$ret0", "$ret1") := (map.get (![#Indexers] (struct.field_ref #State #"indexer"%go (globals.get #state))) (![#stringT] "indexName")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("indexFunc" <-[#IndexFunc] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      return: (#slice.nil, let: "$a0" := #"index %q does not exist"%go in
       let: "$a1" := ((let: "$sl0" := (interface.make #stringT.id (![#stringT] "indexName")) in
       slice.literal #interfaceT ["$sl0"])) in
       (func_call #fmt.Errorf) "$a0" "$a1")
    else do:  #());;;
    let: "err" := (mem.alloc (type.zero_val #error)) in
    let: "indexedValues" := (mem.alloc (type.zero_val #sliceT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#interfaceT] "obj") in
    (![#IndexFunc] "indexFunc") "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("indexedValues" <-[#sliceT] "$r0");;;
    do:  ("err" <-[#error] "$r1");;;
    (if: (~ (interface.eq (![#error] "err") #interface.nil))
    then return: (#slice.nil, ![#error] "err")
    else do:  #());;;
    (if: (let: "$a0" := (![#sliceT] "indexedValues") in
    slice.len "$a0") = #(W64 0)
    then return: (#slice.nil, #interface.nil)
    else do:  #());;;
    let: "indexedValueSet" := (mem.alloc (type.zero_val (type.mapT #stringT (type.structT [
    ])))) in
    let: "$r0" := (map.make #stringT (type.structT [
    ])) in
    do:  ("indexedValueSet" <-[type.mapT #stringT (type.structT [
    ])] "$r0");;;
    let: "$range" := (![#sliceT] "indexedValues") in
    (let: "v" := (mem.alloc (type.zero_val #stringT)) in
    slice.for_range #stringT "$range" (λ: "$key" "$value",
      do:  ("v" <-[#stringT] "$value");;;
      do:  "$key";;;
      let: "$r0" := (struct.make (type.structT [
      ]) [{
      }]) in
      do:  (map.insert (![type.mapT #stringT (type.structT [
      ])] "indexedValueSet") (![#stringT] "v") "$r0")));;;
    let: "items" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$range" := (let: "$a0" := (![#stringT] "kind") in
    let: "$a1" := #v1.NamespaceAll in
    (func_call #objList) "$a0" "$a1") in
    (let: "val" := (mem.alloc (type.zero_val #interfaceT)) in
    slice.for_range #interfaceT "$range" (λ: "$key" "$value",
      do:  ("val" <-[#interfaceT] "$value");;;
      do:  "$key";;;
      let: "err" := (mem.alloc (type.zero_val #error)) in
      let: "values" := (mem.alloc (type.zero_val #sliceT)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#interfaceT] "val") in
      (![#IndexFunc] "indexFunc") "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("values" <-[#sliceT] "$r0");;;
      do:  ("err" <-[#error] "$r1");;;
      (if: (~ (interface.eq (![#error] "err") #interface.nil))
      then return: (#slice.nil, ![#error] "err")
      else do:  #());;;
      let: "$range" := (![#sliceT] "values") in
      (let: "v" := (mem.alloc (type.zero_val #stringT)) in
      slice.for_range #stringT "$range" (λ: "$key" "$value",
        do:  ("v" <-[#stringT] "$value");;;
        do:  "$key";;;
        (let: "match" := (mem.alloc (type.zero_val #boolT)) in
        let: ("$ret0", "$ret1") := (map.get (![type.mapT #stringT (type.structT [
        ])] "indexedValueSet") (![#stringT] "v")) in
        let: "$r0" := "$ret0" in
        let: "$r1" := "$ret1" in
        do:  "$r0";;;
        do:  ("match" <-[#boolT] "$r1");;;
        (if: ![#boolT] "match"
        then
          let: "$r0" := (let: "$a0" := (![#sliceT] "items") in
          let: "$a1" := ((let: "$sl0" := (![#interfaceT] "val") in
          slice.literal #interfaceT ["$sl0"])) in
          (slice.append #interfaceT) "$a0" "$a1") in
          do:  ("items" <-[#sliceT] "$r0");;;
          break: #()
        else do:  #()))))));;;
    return: (![#sliceT] "items", #interface.nil)).

Definition ByIndex : go_string := "kubernetes_model/simpleapiserver.ByIndex"%go.

(* Objects returned here must be treated as read-only.

   go: api_server.go:342:6 *)
Definition ByIndexⁱᵐᵖˡ : val :=
  λ: "kind" "indexName" "indexedValue",
    exception_do (let: "indexedValue" := (mem.alloc "indexedValue") in
    let: "indexName" := (mem.alloc "indexName") in
    let: "kind" := (mem.alloc "kind") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "indexFunc" := (mem.alloc (type.zero_val #IndexFunc)) in
    let: ("$ret0", "$ret1") := (map.get (![#Indexers] (struct.field_ref #State #"indexer"%go (globals.get #state))) (![#stringT] "indexName")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("indexFunc" <-[#IndexFunc] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      return: (#slice.nil, let: "$a0" := #"index %q does not exist"%go in
       let: "$a1" := ((let: "$sl0" := (interface.make #stringT.id (![#stringT] "indexName")) in
       slice.literal #interfaceT ["$sl0"])) in
       (func_call #fmt.Errorf) "$a0" "$a1")
    else do:  #());;;
    let: "items" := (mem.alloc (type.zero_val #sliceT)) in
    let: "listed" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#stringT] "kind") in
    let: "$a1" := #v1.NamespaceAll in
    (func_call #objList) "$a0" "$a1") in
    do:  ("listed" <-[#sliceT] "$r0");;;
    let: "$range" := (![#sliceT] "listed") in
    (let: "val" := (mem.alloc (type.zero_val #interfaceT)) in
    slice.for_range #interfaceT "$range" (λ: "$key" "$value",
      do:  ("val" <-[#interfaceT] "$value");;;
      do:  "$key";;;
      let: "err" := (mem.alloc (type.zero_val #error)) in
      let: "values" := (mem.alloc (type.zero_val #sliceT)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#interfaceT] "val") in
      (![#IndexFunc] "indexFunc") "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("values" <-[#sliceT] "$r0");;;
      do:  ("err" <-[#error] "$r1");;;
      (if: (~ (interface.eq (![#error] "err") #interface.nil))
      then return: (#slice.nil, ![#error] "err")
      else do:  #());;;
      let: "$range" := (![#sliceT] "values") in
      (let: "v" := (mem.alloc (type.zero_val #stringT)) in
      slice.for_range #stringT "$range" (λ: "$key" "$value",
        do:  ("v" <-[#stringT] "$value");;;
        do:  "$key";;;
        (if: (![#stringT] "v") = (![#stringT] "indexedValue")
        then
          let: "$r0" := (let: "$a0" := (![#sliceT] "items") in
          let: "$a1" := ((let: "$sl0" := (![#interfaceT] "val") in
          slice.literal #interfaceT ["$sl0"])) in
          (slice.append #interfaceT) "$a0" "$a1") in
          do:  ("items" <-[#sliceT] "$r0");;;
          break: #()
        else do:  #())))));;;
    return: (![#sliceT] "items", #interface.nil)).

Definition PodGet : go_string := "kubernetes_model/simpleapiserver.PodGet"%go.

(* Object returned here must be treated as read-only.

   go: api_server.go:366:6 *)
Definition PodGetⁱᵐᵖˡ : val :=
  λ: "namespace" "name",
    exception_do (let: "name" := (mem.alloc "name") in
    let: "namespace" := (mem.alloc "namespace") in
    let: "key" := (mem.alloc (type.zero_val #KKey)) in
    let: "$r0" := (let: "$Kind" := #"Pod"%go in
    let: "$Namespace" := (![#stringT] "namespace") in
    let: "$Name" := (![#stringT] "name") in
    struct.make #KKey [{
      "Kind" ::= "$Kind";
      "Name" ::= "$Name";
      "Namespace" ::= "$Namespace"
    }]) in
    do:  ("key" <-[#KKey] "$r0");;;
    let: "exists" := (mem.alloc (type.zero_val #boolT)) in
    let: "obj" := (mem.alloc (type.zero_val #interfaceT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#KKey] "key") in
    (func_call #objGet) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("obj" <-[#interfaceT] "$r0");;;
    do:  ("exists" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "exists"))
    then
      return: (#null, interface.make #(ptrT.id errors.StatusError.id) (let: "$a0" := (let: "$a0" := #"pod"%go in
       (func_call #v1.Resource) "$a0") in
       let: "$a1" := (![#stringT] "name") in
       (func_call #errors.NewNotFound) "$a0" "$a1"))
    else do:  #());;;
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "pod" := (mem.alloc (type.zero_val #ptrT)) in
    let: ("$ret0", "$ret1") := (interface.checked_type_assert #ptrT (![#interfaceT] "obj") #(ptrT.id v1.Pod.id)) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("pod" <-[#ptrT] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      return: (#null, let: "$a0" := #"state entry for pod %s/%s is not a *v1.Pod"%go in
       let: "$a1" := ((let: "$sl0" := (interface.make #stringT.id (![#stringT] "namespace")) in
       let: "$sl1" := (interface.make #stringT.id (![#stringT] "name")) in
       slice.literal #interfaceT ["$sl0"; "$sl1"])) in
       (func_call #fmt.Errorf) "$a0" "$a1")
    else do:  #());;;
    return: (![#ptrT] "pod", #interface.nil)).

Definition PodList : go_string := "kubernetes_model/simpleapiserver.PodList"%go.

Definition PodCreate : go_string := "kubernetes_model/simpleapiserver.PodCreate"%go.

Definition PodUpdate : go_string := "kubernetes_model/simpleapiserver.PodUpdate"%go.

Definition PodDelete : go_string := "kubernetes_model/simpleapiserver.PodDelete"%go.

(* go: api_server.go:436:6 *)
Definition PodDeleteⁱᵐᵖˡ : val :=
  λ: "namespace" "name",
    exception_do (let: "name" := (mem.alloc "name") in
    let: "namespace" := (mem.alloc "namespace") in
    let: "key" := (mem.alloc (type.zero_val #KKey)) in
    let: "$r0" := (let: "$Kind" := #"Pod"%go in
    let: "$Namespace" := (![#stringT] "namespace") in
    let: "$Name" := (![#stringT] "name") in
    struct.make #KKey [{
      "Kind" ::= "$Kind";
      "Name" ::= "$Name";
      "Namespace" ::= "$Namespace"
    }]) in
    do:  ("key" <-[#KKey] "$r0");;;
    return: (let: "$a0" := (![#KKey] "key") in
     (func_call #objDelete) "$a0")).

Definition ReplicaSetGet : go_string := "kubernetes_model/simpleapiserver.ReplicaSetGet"%go.

Definition vars' : list (go_string * go_type) := [].

Axiom controllerUIDIndexⁱᵐᵖˡ : val.

Axiom objGetⁱᵐᵖˡ : val.

Axiom objListⁱᵐᵖˡ : val.

Axiom objListBySelectorⁱᵐᵖˡ : val.

Axiom randomSuffixⁱᵐᵖˡ : val.

Axiom deepCopyⁱᵐᵖˡ : val.

Axiom objCreateⁱᵐᵖˡ : val.

Axiom objUpdateⁱᵐᵖˡ : val.

Axiom objDeleteⁱᵐᵖˡ : val.

Axiom PodListⁱᵐᵖˡ : val.

Axiom PodCreateⁱᵐᵖˡ : val.

Axiom PodUpdateⁱᵐᵖˡ : val.

Axiom ReplicaSetGetⁱᵐᵖˡ : val.

Definition functions' : list (go_string * val) := [(namespaceIndex, namespaceIndexⁱᵐᵖˡ); (podControllerUIDIndex, podControllerUIDIndexⁱᵐᵖˡ); (controllerUIDIndex, controllerUIDIndexⁱᵐᵖˡ); (Init, Initⁱᵐᵖˡ); (OrphanPodIndexKeyForNamespace, OrphanPodIndexKeyForNamespaceⁱᵐᵖˡ); (objGet, objGetⁱᵐᵖˡ); (objList, objListⁱᵐᵖˡ); (filterByLabelSelector, filterByLabelSelectorⁱᵐᵖˡ); (objListBySelector, objListBySelectorⁱᵐᵖˡ); (randomSuffix, randomSuffixⁱᵐᵖˡ); (deepCopy, deepCopyⁱᵐᵖˡ); (objCreate, objCreateⁱᵐᵖˡ); (objUpdate, objUpdateⁱᵐᵖˡ); (objDelete, objDeleteⁱᵐᵖˡ); (Index, Indexⁱᵐᵖˡ); (ByIndex, ByIndexⁱᵐᵖˡ); (PodGet, PodGetⁱᵐᵖˡ); (PodList, PodListⁱᵐᵖˡ); (PodCreate, PodCreateⁱᵐᵖˡ); (PodUpdate, PodUpdateⁱᵐᵖˡ); (PodDelete, PodDeleteⁱᵐᵖˡ); (ReplicaSetGet, ReplicaSetGetⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [(KKey.id, []); (ptrT.id KKey.id, []); (IndexFunc.id, []); (ptrT.id IndexFunc.id, []); (Indexers.id, []); (ptrT.id Indexers.id, []); (State.id, []); (ptrT.id State.id, [])].

#[global] Instance info' : PkgInfo simpleapiserver.simpleapiserver :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.fmt.fmt; code.math.rand.rand; code.strconv.strconv; code.sync.sync; code.time.time; code.k8s_io.api.apps.v1.v1; code.k8s_io.api.core.v1.v1; code.k8s_io.apimachinery.pkg.api.errors.errors; code.k8s_io.apimachinery.pkg.api.meta.meta; code.k8s_io.apimachinery.pkg.apis.meta.v1.v1; code.k8s_io.apimachinery.pkg.labels.labels; code.k8s_io.apimachinery.pkg.runtime.schema.schema; code.k8s_io.apimachinery.pkg.types.types];
  |}.

Axiom _'init : val.

Definition initialize' : val :=
  λ: <>,
    package.init #simpleapiserver.simpleapiserver (λ: <>,
      exception_do (do:  (types.initialize' #());;;
      do:  (schema.initialize' #());;;
      do:  (labels.initialize' #());;;
      do:  (v1.initialize' #());;;
      do:  (meta.initialize' #());;;
      do:  (errors.initialize' #());;;
      do:  (v1.initialize' #());;;
      do:  (v1.initialize' #());;;
      do:  (time.initialize' #());;;
      do:  (sync.initialize' #());;;
      do:  (strconv.initialize' #());;;
      do:  (rand.initialize' #());;;
      do:  (fmt.initialize' #());;;
      do:  (package.alloc simpleapiserver.simpleapiserver #());;;
      do:  (nameRand'init #()))
      ).

End code.
End simpleapiserver.
