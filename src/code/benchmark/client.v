(* autogenerated from benchmark/client *)
Require Export New.code.k8s_io.api.core.v1.
Require Export New.code.k8s_io.apimachinery.pkg.apis.meta.v1.
Require Export New.code.kubernetes_model.apimodel.

From New.golang Require Import defn.
Definition client : go_string := "benchmark/client".

Module client.

Section code.
Context `{ffi_syntax}.


Definition CreateThenDeletePods : go_string := "benchmark/client.CreateThenDeletePods"%go.

(* go: client.go:10:6 *)
Definition CreateThenDeletePodsⁱᵐᵖˡ : val :=
  λ: "state" "pods",
    exception_do (let: "pods" := (mem.alloc "pods") in
    let: "state" := (mem.alloc "state") in
    let: "createdPodKeys" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make3 #apimodel.KKey #(W64 0) (let: "$a0" := (![#sliceT] "pods") in
    slice.len "$a0")) in
    do:  ("createdPodKeys" <-[#sliceT] "$r0");;;
    let: "$range" := (![#sliceT] "pods") in
    (let: "pod" := (mem.alloc (type.zero_val #ptrT)) in
    slice.for_range #ptrT "$range" (λ: "$key" "$value",
      do:  ("pod" <-[#ptrT] "$value");;;
      do:  "$key";;;
      let: "podToCreate" := (mem.alloc (type.zero_val #ptrT)) in
      let: "$r0" := ((method_call #(ptrT.id v1.Pod.id) #"DeepCopy"%go (![#ptrT] "pod")) #()) in
      do:  ("podToCreate" <-[#ptrT] "$r0");;;
      let: "$r0" := #""%go in
      do:  ((struct.field_ref #v1.ObjectMeta #"Name"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "podToCreate"))) <-[#stringT] "$r0");;;
      let: "err" := (mem.alloc (type.zero_val #error)) in
      let: "createdPod" := (mem.alloc (type.zero_val #ptrT)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"Namespace"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "podToCreate")))) in
      let: "$a1" := (![#ptrT] "podToCreate") in
      (method_call #(ptrT.id apimodel.State.id) #"PodCreate"%go (![#ptrT] "state")) "$a0" "$a1") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("createdPod" <-[#ptrT] "$r0");;;
      do:  ("err" <-[#error] "$r1");;;
      (if: (~ (interface.eq (![#error] "err") #interface.nil))
      then return: (![#error] "err")
      else do:  #());;;
      let: "$r0" := (let: "$a0" := (![#sliceT] "createdPodKeys") in
      let: "$a1" := ((let: "$sl0" := (let: "$Kind" := #"Pod"%go in
      let: "$Namespace" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"Namespace"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "createdPod")))) in
      let: "$Name" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"Name"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "createdPod")))) in
      struct.make #apimodel.KKey [{
        "Kind" ::= "$Kind";
        "Name" ::= "$Name";
        "Namespace" ::= "$Namespace"
      }]) in
      slice.literal #apimodel.KKey ["$sl0"])) in
      (slice.append #apimodel.KKey) "$a0" "$a1") in
      do:  ("createdPodKeys" <-[#sliceT] "$r0")));;;
    let: "createdPods" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make3 #ptrT #(W64 0) (let: "$a0" := (![#sliceT] "createdPodKeys") in
    slice.len "$a0")) in
    do:  ("createdPods" <-[#sliceT] "$r0");;;
    let: "$range" := (![#sliceT] "createdPodKeys") in
    (let: "key" := (mem.alloc (type.zero_val #apimodel.KKey)) in
    slice.for_range #apimodel.KKey "$range" (λ: "$key" "$value",
      do:  ("key" <-[#apimodel.KKey] "$value");;;
      do:  "$key";;;
      let: "err" := (mem.alloc (type.zero_val #error)) in
      let: "pod" := (mem.alloc (type.zero_val #ptrT)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#stringT] (struct.field_ref #apimodel.KKey #"Namespace"%go "key")) in
      let: "$a1" := (![#stringT] (struct.field_ref #apimodel.KKey #"Name"%go "key")) in
      (method_call #(ptrT.id apimodel.State.id) #"PodGet"%go (![#ptrT] "state")) "$a0" "$a1") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("pod" <-[#ptrT] "$r0");;;
      do:  ("err" <-[#error] "$r1");;;
      (if: (~ (interface.eq (![#error] "err") #interface.nil))
      then return: (![#error] "err")
      else do:  #());;;
      let: "$r0" := (let: "$a0" := (![#sliceT] "createdPods") in
      let: "$a1" := ((let: "$sl0" := (![#ptrT] "pod") in
      slice.literal #ptrT ["$sl0"])) in
      (slice.append #ptrT) "$a0" "$a1") in
      do:  ("createdPods" <-[#sliceT] "$r0")));;;
    let: "$range" := (![#sliceT] "createdPods") in
    (let: "pod" := (mem.alloc (type.zero_val #ptrT)) in
    slice.for_range #ptrT "$range" (λ: "$key" "$value",
      do:  ("pod" <-[#ptrT] "$value");;;
      do:  "$key";;;
      let: "uid" := (mem.alloc (type.zero_val #types.UID)) in
      let: "$r0" := (![#types.UID] (struct.field_ref #v1.ObjectMeta #"UID"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "pod")))) in
      do:  ("uid" <-[#types.UID] "$r0");;;
      let: "resourceVersion" := (mem.alloc (type.zero_val #stringT)) in
      let: "$r0" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"ResourceVersion"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "pod")))) in
      do:  ("resourceVersion" <-[#stringT] "$r0");;;
      let: "options" := (mem.alloc (type.zero_val #v1.DeleteOptions)) in
      let: "$r0" := (let: "$Preconditions" := (mem.alloc (let: "$UID" := "uid" in
      let: "$ResourceVersion" := "resourceVersion" in
      struct.make #v1.Preconditions [{
        "UID" ::= "$UID";
        "ResourceVersion" ::= "$ResourceVersion"
      }])) in
      struct.make #v1.DeleteOptions [{
        "TypeMeta" ::= type.zero_val #v1.TypeMeta;
        "GracePeriodSeconds" ::= type.zero_val #ptrT;
        "Preconditions" ::= "$Preconditions";
        "OrphanDependents" ::= type.zero_val #ptrT;
        "PropagationPolicy" ::= type.zero_val #ptrT;
        "DryRun" ::= type.zero_val #sliceT;
        "IgnoreStoreReadErrorWithClusterBreakingPotential" ::= type.zero_val #ptrT
      }]) in
      do:  ("options" <-[#v1.DeleteOptions] "$r0");;;
      (let: "err" := (mem.alloc (type.zero_val #error)) in
      let: "$r0" := (let: "$a0" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"Namespace"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "pod")))) in
      let: "$a1" := (![#stringT] (struct.field_ref #v1.ObjectMeta #"Name"%go (struct.field_ref #v1.Pod #"ObjectMeta"%go (![#ptrT] "pod")))) in
      let: "$a2" := (![#v1.DeleteOptions] "options") in
      (method_call #(ptrT.id apimodel.State.id) #"PodDelete"%go (![#ptrT] "state")) "$a0" "$a1" "$a2") in
      do:  ("err" <-[#error] "$r0");;;
      (if: (~ (interface.eq (![#error] "err") #interface.nil))
      then return: (![#error] "err")
      else do:  #()))));;;
    return: (#interface.nil)).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [(CreateThenDeletePods, CreateThenDeletePodsⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [].

#[global] Instance info' : PkgInfo client.client :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.kubernetes_model.apimodel.apimodel; code.k8s_io.api.core.v1.v1; code.k8s_io.apimachinery.pkg.apis.meta.v1.v1];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #client.client (λ: <>,
      exception_do (do:  (v1.initialize' #());;;
      do:  (v1.initialize' #());;;
      do:  (apimodel.initialize' #());;;
      do:  (package.alloc client.client #()))
      ).

End code.
End client.
