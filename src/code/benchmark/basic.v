(* autogenerated from benchmark/basic *)

From New.golang Require Import defn.
Definition basic : go_string := "benchmark/basic".

Module basic.

Section code.
Context `{ffi_syntax}.


Definition add : go_string := "benchmark/basic.add"%go.

(* go: basic.go:3:6 *)
Definition addⁱᵐᵖˡ : val :=
  λ: "a" "b",
    exception_do (let: "b" := (mem.alloc "b") in
    let: "a" := (mem.alloc "a") in
    return: ((![#intT] "a") + (![#intT] "b"))).

Definition max : go_string := "benchmark/basic.max"%go.

(* go: basic.go:7:6 *)
Definition maxⁱᵐᵖˡ : val :=
  λ: "a" "b",
    exception_do (let: "b" := (mem.alloc "b") in
    let: "a" := (mem.alloc "a") in
    (if: int_gt (![#intT] "a") (![#intT] "b")
    then return: (![#intT] "a")
    else do:  #());;;
    return: (![#intT] "b")).

Definition factorial : go_string := "benchmark/basic.factorial"%go.

(* go: basic.go:14:6 *)
Definition factorialⁱᵐᵖˡ : val :=
  λ: "n",
    exception_do (let: "n" := (mem.alloc "n") in
    (if: int_lt (![#intT] "n") #(W64 0)
    then return: (#(W64 0))
    else do:  #());;;
    let: "result" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := #(W64 1) in
    do:  ("result" <-[#intT] "$r0");;;
    (let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := #(W64 2) in
    do:  ("i" <-[#intT] "$r0");;;
    (for: (λ: <>, int_leq (![#intT] "i") (![#intT] "n")); (λ: <>, do:  ("i" <-[#intT] ((![#intT] "i") + #(W64 1)))) := λ: <>,
      do:  ("result" <-[#intT] ((![#intT] "result") * (![#intT] "i")))));;;
    return: (![#intT] "result")).

Definition sum : go_string := "benchmark/basic.sum"%go.

(* go: basic.go:25:6 *)
Definition sumⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "total" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := #(W64 0) in
    do:  ("total" <-[#intT] "$r0");;;
    let: "$range" := (![#sliceT] "values") in
    (let: "value" := (mem.alloc (type.zero_val #intT)) in
    slice.for_range #intT "$range" (λ: "$key" "$value",
      do:  ("value" <-[#intT] "$value");;;
      do:  "$key";;;
      do:  ("total" <-[#intT] ((![#intT] "total") + (![#intT] "value")))));;;
    return: (![#intT] "total")).

Definition reverseInts : go_string := "benchmark/basic.reverseInts"%go.

(* go: basic.go:33:6 *)
Definition reverseIntsⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "result" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make2 #intT (let: "$a0" := (![#sliceT] "values") in
    slice.len "$a0")) in
    do:  ("result" <-[#sliceT] "$r0");;;
    let: "$range" := (![#sliceT] "values") in
    (let: "value" := (mem.alloc (type.zero_val #intT)) in
    let: "i" := (mem.alloc (type.zero_val #intT)) in
    slice.for_range #intT "$range" (λ: "$key" "$value",
      do:  ("value" <-[#intT] "$value");;;
      do:  ("i" <-[#intT] "$key");;;
      let: "$r0" := (![#intT] "value") in
      do:  ((slice.elem_ref #intT (![#sliceT] "result") (((let: "$a0" := (![#sliceT] "values") in
      slice.len "$a0") - #(W64 1)) - (![#intT] "i"))) <-[#intT] "$r0")));;;
    return: (![#sliceT] "result")).

Definition filterPositive : go_string := "benchmark/basic.filterPositive"%go.

(* go: basic.go:41:6 *)
Definition filterPositiveⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "result" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make3 #intT #(W64 0) (let: "$a0" := (![#sliceT] "values") in
    slice.len "$a0")) in
    do:  ("result" <-[#sliceT] "$r0");;;
    let: "$range" := (![#sliceT] "values") in
    (let: "value" := (mem.alloc (type.zero_val #intT)) in
    slice.for_range #intT "$range" (λ: "$key" "$value",
      do:  ("value" <-[#intT] "$value");;;
      do:  "$key";;;
      (if: int_gt (![#intT] "value") #(W64 0)
      then
        let: "$r0" := (let: "$a0" := (![#sliceT] "result") in
        let: "$a1" := ((let: "$sl0" := (![#intT] "value") in
        slice.literal #intT ["$sl0"])) in
        (slice.append #intT) "$a0" "$a1") in
        do:  ("result" <-[#sliceT] "$r0")
      else do:  #())));;;
    return: (![#sliceT] "result")).

Definition frequencies : go_string := "benchmark/basic.frequencies"%go.

(* go: basic.go:51:6 *)
Definition frequenciesⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "counts" := (mem.alloc (type.zero_val (type.mapT #stringT #intT))) in
    let: "$r0" := (map.make #stringT #intT) in
    do:  ("counts" <-[type.mapT #stringT #intT] "$r0");;;
    let: "$range" := (![#sliceT] "values") in
    (let: "value" := (mem.alloc (type.zero_val #stringT)) in
    slice.for_range #stringT "$range" (λ: "$key" "$value",
      do:  ("value" <-[#stringT] "$value");;;
      do:  "$key";;;
      do:  (map.insert (![type.mapT #stringT #intT] "counts") (![#stringT] "value") ((Fst (map.get (![type.mapT #stringT #intT] "counts") (![#stringT] "value"))) + #(W64 1)))));;;
    return: (![type.mapT #stringT #intT] "counts")).

Definition binarySearch : go_string := "benchmark/basic.binarySearch"%go.

(* go: basic.go:59:6 *)
Definition binarySearchⁱᵐᵖˡ : val :=
  λ: "sorted" "target",
    exception_do (let: "target" := (mem.alloc "target") in
    let: "sorted" := (mem.alloc "sorted") in
    let: "low" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := #(W64 0) in
    do:  ("low" <-[#intT] "$r0");;;
    let: "high" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := ((let: "$a0" := (![#sliceT] "sorted") in
    slice.len "$a0") - #(W64 1)) in
    do:  ("high" <-[#intT] "$r0");;;
    (for: (λ: <>, int_leq (![#intT] "low") (![#intT] "high")); (λ: <>, #()) := λ: <>,
      let: "mid" := (mem.alloc (type.zero_val #intT)) in
      let: "$r0" := ((![#intT] "low") + (((![#intT] "high") - (![#intT] "low")) `quots` #(W64 2))) in
      do:  ("mid" <-[#intT] "$r0");;;
      (if: (![#intT] (slice.elem_ref #intT (![#sliceT] "sorted") (![#intT] "mid"))) = (![#intT] "target")
      then return: (![#intT] "mid")
      else do:  #());;;
      (if: int_lt (![#intT] (slice.elem_ref #intT (![#sliceT] "sorted") (![#intT] "mid"))) (![#intT] "target")
      then
        let: "$r0" := ((![#intT] "mid") + #(W64 1)) in
        do:  ("low" <-[#intT] "$r0")
      else
        let: "$r0" := ((![#intT] "mid") - #(W64 1)) in
        do:  ("high" <-[#intT] "$r0")));;;
    return: (#(W64 (- 1)))).

Definition mergeCounts : go_string := "benchmark/basic.mergeCounts"%go.

(* go: basic.go:76:6 *)
Definition mergeCountsⁱᵐᵖˡ : val :=
  λ: "left" "right",
    exception_do (let: "right" := (mem.alloc "right") in
    let: "left" := (mem.alloc "left") in
    let: "result" := (mem.alloc (type.zero_val (type.mapT #stringT #intT))) in
    let: "$r0" := (map.make #stringT #intT) in
    do:  ("result" <-[type.mapT #stringT #intT] "$r0");;;
    let: "$range" := (![type.mapT #stringT #intT] "left") in
    (let: "value" := (mem.alloc (type.zero_val #intT)) in
    let: "key" := (mem.alloc (type.zero_val #stringT)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("value" <-[#intT] "$value");;;
      do:  ("key" <-[#stringT] "$key");;;
      let: "$r0" := (![#intT] "value") in
      do:  (map.insert (![type.mapT #stringT #intT] "result") (![#stringT] "key") "$r0")));;;
    let: "$range" := (![type.mapT #stringT #intT] "right") in
    (let: "value" := (mem.alloc (type.zero_val #intT)) in
    let: "key" := (mem.alloc (type.zero_val #stringT)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("value" <-[#intT] "$value");;;
      do:  ("key" <-[#stringT] "$key");;;
      do:  (map.insert (![type.mapT #stringT #intT] "result") (![#stringT] "key") ((Fst (map.get (![type.mapT #stringT #intT] "result") (![#stringT] "key"))) + (![#intT] "value")))));;;
    return: (![type.mapT #stringT #intT] "result")).

Definition uniqueStrings : go_string := "benchmark/basic.uniqueStrings"%go.

(* go: basic.go:87:6 *)
Definition uniqueStringsⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "seen" := (mem.alloc (type.zero_val (type.mapT #stringT #boolT))) in
    let: "$r0" := (map.make #stringT #boolT) in
    do:  ("seen" <-[type.mapT #stringT #boolT] "$r0");;;
    let: "result" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make3 #stringT #(W64 0) (let: "$a0" := (![#sliceT] "values") in
    slice.len "$a0")) in
    do:  ("result" <-[#sliceT] "$r0");;;
    let: "$range" := (![#sliceT] "values") in
    (let: "value" := (mem.alloc (type.zero_val #stringT)) in
    slice.for_range #stringT "$range" (λ: "$key" "$value",
      do:  ("value" <-[#stringT] "$value");;;
      do:  "$key";;;
      (if: (~ (Fst (map.get (![type.mapT #stringT #boolT] "seen") (![#stringT] "value"))))
      then
        let: "$r0" := #true in
        do:  (map.insert (![type.mapT #stringT #boolT] "seen") (![#stringT] "value") "$r0");;;
        let: "$r0" := (let: "$a0" := (![#sliceT] "result") in
        let: "$a1" := ((let: "$sl0" := (![#stringT] "value") in
        slice.literal #stringT ["$sl0"])) in
        (slice.append #stringT) "$a0" "$a1") in
        do:  ("result" <-[#sliceT] "$r0")
      else do:  #())));;;
    return: (![#sliceT] "result")).

Definition selectionSort : go_string := "benchmark/basic.selectionSort"%go.

(* go: basic.go:99:6 *)
Definition selectionSortⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "result" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := #slice.nil in
    let: "$a1" := (![#sliceT] "values") in
    (slice.append #intT) "$a0" "$a1") in
    do:  ("result" <-[#sliceT] "$r0");;;
    (let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := #(W64 0) in
    do:  ("i" <-[#intT] "$r0");;;
    (for: (λ: <>, int_lt (![#intT] "i") (let: "$a0" := (![#sliceT] "result") in
    slice.len "$a0")); (λ: <>, do:  ("i" <-[#intT] ((![#intT] "i") + #(W64 1)))) := λ: <>,
      let: "minIndex" := (mem.alloc (type.zero_val #intT)) in
      let: "$r0" := (![#intT] "i") in
      do:  ("minIndex" <-[#intT] "$r0");;;
      (let: "j" := (mem.alloc (type.zero_val #intT)) in
      let: "$r0" := ((![#intT] "i") + #(W64 1)) in
      do:  ("j" <-[#intT] "$r0");;;
      (for: (λ: <>, int_lt (![#intT] "j") (let: "$a0" := (![#sliceT] "result") in
      slice.len "$a0")); (λ: <>, do:  ("j" <-[#intT] ((![#intT] "j") + #(W64 1)))) := λ: <>,
        (if: int_lt (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "j"))) (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "minIndex")))
        then
          let: "$r0" := (![#intT] "j") in
          do:  ("minIndex" <-[#intT] "$r0")
        else do:  #())));;;
      let: "$r0" := (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "minIndex"))) in
      let: "$r1" := (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "i"))) in
      do:  ((slice.elem_ref #intT (![#sliceT] "result") (![#intT] "i")) <-[#intT] "$r0");;;
      do:  ((slice.elem_ref #intT (![#sliceT] "result") (![#intT] "minIndex")) <-[#intT] "$r1")));;;
    return: (![#sliceT] "result")).

Definition insertionSort : go_string := "benchmark/basic.insertionSort"%go.

(* go: basic.go:113:6 *)
Definition insertionSortⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "result" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := #slice.nil in
    let: "$a1" := (![#sliceT] "values") in
    (slice.append #intT) "$a0" "$a1") in
    do:  ("result" <-[#sliceT] "$r0");;;
    (let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := #(W64 1) in
    do:  ("i" <-[#intT] "$r0");;;
    (for: (λ: <>, int_lt (![#intT] "i") (let: "$a0" := (![#sliceT] "result") in
    slice.len "$a0")); (λ: <>, do:  ("i" <-[#intT] ((![#intT] "i") + #(W64 1)))) := λ: <>,
      let: "value" := (mem.alloc (type.zero_val #intT)) in
      let: "$r0" := (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "i"))) in
      do:  ("value" <-[#intT] "$r0");;;
      let: "j" := (mem.alloc (type.zero_val #intT)) in
      let: "$r0" := ((![#intT] "i") - #(W64 1)) in
      do:  ("j" <-[#intT] "$r0");;;
      (for: (λ: <>, (int_geq (![#intT] "j") #(W64 0)) && (int_gt (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "j"))) (![#intT] "value"))); (λ: <>, #()) := λ: <>,
        let: "$r0" := (![#intT] (slice.elem_ref #intT (![#sliceT] "result") (![#intT] "j"))) in
        do:  ((slice.elem_ref #intT (![#sliceT] "result") ((![#intT] "j") + #(W64 1))) <-[#intT] "$r0");;;
        do:  ("j" <-[#intT] ((![#intT] "j") - #(W64 1))));;;
      let: "$r0" := (![#intT] "value") in
      do:  ((slice.elem_ref #intT (![#sliceT] "result") ((![#intT] "j") + #(W64 1))) <-[#intT] "$r0")));;;
    return: (![#sliceT] "result")).

Definition quickSort : go_string := "benchmark/basic.quickSort"%go.

Definition quickSortRange : go_string := "benchmark/basic.quickSortRange"%go.

(* go: basic.go:127:6 *)
Definition quickSortⁱᵐᵖˡ : val :=
  λ: "values",
    exception_do (let: "values" := (mem.alloc "values") in
    let: "result" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := #slice.nil in
    let: "$a1" := (![#sliceT] "values") in
    (slice.append #intT) "$a0" "$a1") in
    do:  ("result" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#sliceT] "result") in
    let: "$a1" := #(W64 0) in
    let: "$a2" := ((let: "$a0" := (![#sliceT] "result") in
    slice.len "$a0") - #(W64 1)) in
    (func_call #quickSortRange) "$a0" "$a1" "$a2");;;
    return: (![#sliceT] "result")).

Definition partition : go_string := "benchmark/basic.partition"%go.

(* go: basic.go:133:6 *)
Definition quickSortRangeⁱᵐᵖˡ : val :=
  λ: "values" "low" "high",
    exception_do (let: "high" := (mem.alloc "high") in
    let: "low" := (mem.alloc "low") in
    let: "values" := (mem.alloc "values") in
    (if: int_geq (![#intT] "low") (![#intT] "high")
    then return: (#())
    else do:  #());;;
    let: "pivot" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "values") in
    let: "$a1" := (![#intT] "low") in
    let: "$a2" := (![#intT] "high") in
    (func_call #partition) "$a0" "$a1" "$a2") in
    do:  ("pivot" <-[#intT] "$r0");;;
    do:  (let: "$a0" := (![#sliceT] "values") in
    let: "$a1" := (![#intT] "low") in
    let: "$a2" := ((![#intT] "pivot") - #(W64 1)) in
    (func_call #quickSortRange) "$a0" "$a1" "$a2");;;
    do:  (let: "$a0" := (![#sliceT] "values") in
    let: "$a1" := ((![#intT] "pivot") + #(W64 1)) in
    let: "$a2" := (![#intT] "high") in
    (func_call #quickSortRange) "$a0" "$a1" "$a2");;;
    return: #()).

(* go: basic.go:142:6 *)
Definition partitionⁱᵐᵖˡ : val :=
  λ: "values" "low" "high",
    exception_do (let: "high" := (mem.alloc "high") in
    let: "low" := (mem.alloc "low") in
    let: "values" := (mem.alloc "values") in
    let: "pivot" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := (![#intT] (slice.elem_ref #intT (![#sliceT] "values") (![#intT] "high"))) in
    do:  ("pivot" <-[#intT] "$r0");;;
    let: "store" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := (![#intT] "low") in
    do:  ("store" <-[#intT] "$r0");;;
    (let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := (![#intT] "low") in
    do:  ("i" <-[#intT] "$r0");;;
    (for: (λ: <>, int_lt (![#intT] "i") (![#intT] "high")); (λ: <>, do:  ("i" <-[#intT] ((![#intT] "i") + #(W64 1)))) := λ: <>,
      (if: int_lt (![#intT] (slice.elem_ref #intT (![#sliceT] "values") (![#intT] "i"))) (![#intT] "pivot")
      then
        let: "$r0" := (![#intT] (slice.elem_ref #intT (![#sliceT] "values") (![#intT] "store"))) in
        let: "$r1" := (![#intT] (slice.elem_ref #intT (![#sliceT] "values") (![#intT] "i"))) in
        do:  ((slice.elem_ref #intT (![#sliceT] "values") (![#intT] "i")) <-[#intT] "$r0");;;
        do:  ((slice.elem_ref #intT (![#sliceT] "values") (![#intT] "store")) <-[#intT] "$r1");;;
        do:  ("store" <-[#intT] ((![#intT] "store") + #(W64 1)))
      else do:  #())));;;
    let: "$r0" := (![#intT] (slice.elem_ref #intT (![#sliceT] "values") (![#intT] "high"))) in
    let: "$r1" := (![#intT] (slice.elem_ref #intT (![#sliceT] "values") (![#intT] "store"))) in
    do:  ((slice.elem_ref #intT (![#sliceT] "values") (![#intT] "store")) <-[#intT] "$r0");;;
    do:  ((slice.elem_ref #intT (![#sliceT] "values") (![#intT] "high")) <-[#intT] "$r1");;;
    return: (![#intT] "store")).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [(add, addⁱᵐᵖˡ); (max, maxⁱᵐᵖˡ); (factorial, factorialⁱᵐᵖˡ); (sum, sumⁱᵐᵖˡ); (reverseInts, reverseIntsⁱᵐᵖˡ); (filterPositive, filterPositiveⁱᵐᵖˡ); (frequencies, frequenciesⁱᵐᵖˡ); (binarySearch, binarySearchⁱᵐᵖˡ); (mergeCounts, mergeCountsⁱᵐᵖˡ); (uniqueStrings, uniqueStringsⁱᵐᵖˡ); (selectionSort, selectionSortⁱᵐᵖˡ); (insertionSort, insertionSortⁱᵐᵖˡ); (quickSort, quickSortⁱᵐᵖˡ); (quickSortRange, quickSortRangeⁱᵐᵖˡ); (partition, partitionⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [].

#[global] Instance info' : PkgInfo basic.basic :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #basic.basic (λ: <>,
      exception_do (do:  (package.alloc basic.basic #()))
      ).

End code.
End basic.
